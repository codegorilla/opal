package main;

import math;
import stdlib;

private const var x: int = 1;

var y: int = 2;

private class Token extends Object {
  private var a: float = 1.5;
  private var x: int = 1;

  def getLexeme () -> String {
    var t: int = 0;
  }
}

def main () -> int {
  println("Hello world!");

  while (x != 5)
    var s: int = 1;

  until (x < 10)
    var t: int = 1;

  if (x == true) {
    var u: int = 2;
  } else if (x == false)
    var v: int = 3;
}
