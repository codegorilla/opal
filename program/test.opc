package main;

import math;
import stdlib;

private const var x: int = 1;
const var y: int = 2;

final class Token extends Object, Number {
  private const var a: float = 1.5;
  private var x: int = 1;

  virtual final def getLexeme () const -> String {
    var t: int = 0;
  }

  virtual final def getToken () -> int {
    var t: int = 0;
  }

}

constexpr def test () -> int {
  println("Hello world!");

  while (x != 5)
    var s: int = 1;

  until (x < 10)
    var t: int = 1;

  if (x == true) {
    var u: int = 2;
  } else if (x == false)
    var v: int = 3;
}
