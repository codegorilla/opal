package main;

import math;
import stdlib;

private const var x: int = cast<int>(1);
private const var y: int = 2;

private final class Token extends Object, Number {
  private const var a: float = 1.5;
  private var x: int = 1;

  virtual final def getLexeme () const -> String {
    var t: int = 0;
  }

  virtual final def getToken () const & -> int {
    var t: int = 0;
  }

}

private constexpr noexcept def test () -> int {
  println("Hello world!");

  while (x != 5)
    var s: int = 1;

  until (x < 10)
    var t = 1;

  if (x == true) {
    var u = 2;
  } else if (x == false)
    var v = 3.14;
}
