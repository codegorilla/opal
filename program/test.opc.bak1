package main;

import math;
import stdlib;

var x: int = 1;

class Token extends Object {
}

def main (argc: int, argv: *char[]) -> int {

}

def myfun1 (s: float, t: uint32) -> *int {

  var r: int = x[2][4];

  var s: int = x->y->z;
  var t: int = x.y.z;

  std.println("hello", null);

  var t: int = 0;

  var a: char[] = "hello";

  var x: char = 'b';

  loop (i=0; i<10; i+=1) {
    x.setWidth(i);
  }

  for (i in list) {
    println(i);
    break;
  }

}
